import React from 'react';
import useTitle from '../../Hooks/useTitle';

const About = () => {
    useTitle('About');
    return (
        <div>
            
        </div>
    );
};

export default About;